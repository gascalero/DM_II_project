## Continuous Variables

- **Date of birth**: Should be converted to a continuous age variable.

- **Healthcare costs**:
    - Contains negative values. Need to interpret the reason behind this. (Could it be related to the 'insurance cost' variable class? Could it be a reimbursement?)
    - Should be scaled due to the magnitude of its range.

- **Mortality rate per 100K**: Contains negative values. Most likely need to impute or change its value.

- **Outliers detected in**:
    - 'Healthcare Costs'
    - 'Incidence Rate per 100K'
    - 'Mortality Rate per 100K'
    - 'Tumor Size (mm)'

## Categorical Variables

- **Marital status**: Should be removed as it is completely empty.

- **Transfusion history**: Should be removed as it only contains null values.

- **Gender**: Contains an additional class 'P'.
    - (Could it be due to an undefined gender or a minority community?)

- **Healthcare Access**: Contains an additional class '?'.
    - Could be due to unknown information due to unique customer factors (e.g., location, rural or urban area?)

- **Urban or Rural**: Contains repeated classes written differently.

- **Diabetes History**: Almost the entire distribution falls into the 'No' class. Does not add much information. Consider removing.

## Feature Engineering

- Is there a correspondence between the classes of 'Insurance cost' and 'Insurance Status'?

- Could 'Obesity BMI' and 'Diet risk' be highly correlated? Both are categorical variables.

---

**Notes for Features_prep_4**:

WHAT DID COME FROM Features_prep_3 ?:

- Remove problematic columns ('Transfusion History', 'Marital Status', 'Smoking History')

- Replace strange values in 'Gender' and 'Healthcare Access'

- Remove rows with missing values, so no imputation is performed in the train set.

- Standardize values in the 'Urban or Rural' column

- Convert boolean variables from string to integers (1, 0)

- Create the 'Age' variable from 'Date of Birth' and remove the latter.

- Winsorize outliers.

- Replace negative values with the column median.

- Scale ONLY numerical columns.

- Create dummies for categorical variables ('Cancer Stage', 'Country', 'Diet Risk', 'Gender', 'Healthcare Access',
    'Insurance Costs', 'Insurance Status', 'Obesity BMI', 'Physical Activity',
    'Screening History', 'Smoking History', 'Treatment Type', 'Urban or Rural')

WHAT TO DO DIFFERENTLY IN Features_prep_4?

[X] Perform custom preprocessing depending on the algorithm (e.g., limit scaling for models sensitive to scale, such as KNN)

[X] Create a new variable 'Cardiometabolic_Risk' that groups Diabetes and Heart Disease History. 'Diabetes History' was moved from binary_columns to the columns_to_delete list. 'Diabetes' and 'Heart Disease History' are kept so the model can better differentiate patterns.

[X] In the split_sets function, I transformed the 'DoB' column to a datatype due to an unknown error that prevented transformation later in the already split X_test.

[X] Added 'Age' to numeric_column to impute missing values with the train_set median.

With these modifications, I obtained an F1 score of 0.51 using logistic regression with max_iter set to 100000 and class_weight balanced.

